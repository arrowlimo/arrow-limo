# Arrow Limousine Management System - Environment Configuration
# Copy this file to .env and fill in your database credentials
# NEVER commit .env to git - .env is in .gitignore

# ============================================================================
# NEON DATABASE CONFIGURATION (Primary - Cloud)
# ============================================================================
# Your Neon PostgreSQL database credentials
# Get these from https://console.neon.tech

DB_HOST=your-neon-host.us-west-2.aws.neon.tech
DB_PORT=5432
DB_NAME=neondb
DB_USER=neondb_owner
DB_PASSWORD=your_neon_password_here
DB_SSLMODE=require

# ============================================================================
# LOCAL DATABASE CONFIGURATION (Optional - for desktop app offline)
# ============================================================================
# If you want to use local PostgreSQL for the desktop app:

LOCAL_DB_NAME=almsdata
LOCAL_DB_USER=postgres
LOCAL_DB_PASSWORD=your_local_password_here

# Also set the generic NEON vars for desktop app use:
NEON_DB_HOST=your-neon-host.us-west-2.aws.neon.tech
NEON_DB_NAME=neondb
NEON_DB_USER=neondb_owner
NEON_DB_PASSWORD=your_neon_password_here

# ============================================================================
# API CONFIGURATION (Optional - for web app)
# ============================================================================
# Backend API URL for web access or sync features
API_URL=https://arrow-limo.onrender.com
API_TIMEOUT=30

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
# Set to true for verbose logging during troubleshooting
DEBUG=false

# Set your preferred timezone
TIMEZONE=America/Edmonton

# Email notifications (optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
NOTIFICATIONS_ENABLED=false
